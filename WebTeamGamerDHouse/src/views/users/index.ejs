<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('../partials/head.ejs') %> 
    <title>TeamGamerDHouse</title>
  </head>
  <body>
    <header id="main-header">
      <%- include('../partials/header.ejs') %>
    </header>

      <!--Empieza el contenido-->
    <main>
      <picture id="portada">
        <img src="/image/portada/portada-juegos.jpg"  alt="portada-juegos">
      </picture>
      
      <section class="products" id="gamesps">
          <h2>GAMES</h2>

          <ul class="product-list">
              <% if(Array.isArray(productos)){%>
                <% for(let i=0;i < productos.length; i++){%>

                  <li class="product-card">
                    <div class="card" style="width: 18rem;">
                      <img src="/image/imagenArticulos/<%=productos[i].imagen%>" class="card-img-top" alt="...">
                      <div class="card-body">
                        <h5 class="card-title text-center "><%= productos[i].nombre%></h5>
                        <h5 class="card-title">Precio: $<%= productos[i].precio%></h5>
                        <a href="/producto/detalle/<%=productos[i].id%> " class="btn btn-primary">Ver Detalle</a>
                      </div>
                    </div>
                    <!-- <picture>
                    <img src="/image/imagenArticulos/<%=productos[i].imagen%>"  alt="">
                    </picture>
                    <article>
                      <h3><%= productos[i].nombre%> </h3>
                      <p>Precio:  <%= productos[i].precio%></p>
                        <nav>
                          <a href="/producto/detalle/<%=productos[i].id%> "> <i class="fa-solid fa-eye"></i></a>
                        </nav>
                    </article> -->
                  </li>
          <% } %> 
          <% } %> 
        
    </main>
    
    
   
   <footer>
    
    <%- include('../partials/footer.ejs')  %> 
        

    </footer>
</body>
</html>